import pulumi

def create():
	#db_cfg = config.require_object('db')
	#db = do.Droplet(
	#	db_cfg['name'],
	#	monitoring=True,
	#	name=db_cfg['name'],
	#	size=db_cfg['size'],
	#	image=db_cfg['image'],
	#	region=config.require('region'),
	#)
	#return db
	pass
